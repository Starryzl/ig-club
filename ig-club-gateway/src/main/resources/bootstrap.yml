spring:
  application:
    name: ig-club-gateway-dev
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 192.168.254.155:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        enabled: true
        server-addr: 192.168.254.155:8848